// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/inventory-service/internal/ent/batchrawmaterial"
	"github.com/bengobox/inventory-service/internal/ent/predicate"
	"github.com/bengobox/inventory-service/internal/ent/productionbatch"
	"github.com/bengobox/inventory-service/internal/ent/qualitycheck"
	"github.com/google/uuid"
)

// ProductionBatchQuery is the builder for querying ProductionBatch entities.
type ProductionBatchQuery struct {
	config
	ctx               *QueryContext
	order             []productionbatch.OrderOption
	inters            []Interceptor
	predicates        []predicate.ProductionBatch
	withRawMaterials  *BatchRawMaterialQuery
	withQualityChecks *QualityCheckQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProductionBatchQuery builder.
func (_q *ProductionBatchQuery) Where(ps ...predicate.ProductionBatch) *ProductionBatchQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ProductionBatchQuery) Limit(limit int) *ProductionBatchQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ProductionBatchQuery) Offset(offset int) *ProductionBatchQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ProductionBatchQuery) Unique(unique bool) *ProductionBatchQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ProductionBatchQuery) Order(o ...productionbatch.OrderOption) *ProductionBatchQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryRawMaterials chains the current query on the "raw_materials" edge.
func (_q *ProductionBatchQuery) QueryRawMaterials() *BatchRawMaterialQuery {
	query := (&BatchRawMaterialClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(productionbatch.Table, productionbatch.FieldID, selector),
			sqlgraph.To(batchrawmaterial.Table, batchrawmaterial.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, productionbatch.RawMaterialsTable, productionbatch.RawMaterialsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryQualityChecks chains the current query on the "quality_checks" edge.
func (_q *ProductionBatchQuery) QueryQualityChecks() *QualityCheckQuery {
	query := (&QualityCheckClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(productionbatch.Table, productionbatch.FieldID, selector),
			sqlgraph.To(qualitycheck.Table, qualitycheck.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, productionbatch.QualityChecksTable, productionbatch.QualityChecksColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProductionBatch entity from the query.
// Returns a *NotFoundError when no ProductionBatch was found.
func (_q *ProductionBatchQuery) First(ctx context.Context) (*ProductionBatch, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{productionbatch.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ProductionBatchQuery) FirstX(ctx context.Context) *ProductionBatch {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProductionBatch ID from the query.
// Returns a *NotFoundError when no ProductionBatch ID was found.
func (_q *ProductionBatchQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{productionbatch.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ProductionBatchQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProductionBatch entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProductionBatch entity is found.
// Returns a *NotFoundError when no ProductionBatch entities are found.
func (_q *ProductionBatchQuery) Only(ctx context.Context) (*ProductionBatch, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{productionbatch.Label}
	default:
		return nil, &NotSingularError{productionbatch.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ProductionBatchQuery) OnlyX(ctx context.Context) *ProductionBatch {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProductionBatch ID in the query.
// Returns a *NotSingularError when more than one ProductionBatch ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ProductionBatchQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{productionbatch.Label}
	default:
		err = &NotSingularError{productionbatch.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ProductionBatchQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProductionBatches.
func (_q *ProductionBatchQuery) All(ctx context.Context) ([]*ProductionBatch, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProductionBatch, *ProductionBatchQuery]()
	return withInterceptors[[]*ProductionBatch](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ProductionBatchQuery) AllX(ctx context.Context) []*ProductionBatch {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProductionBatch IDs.
func (_q *ProductionBatchQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(productionbatch.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ProductionBatchQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ProductionBatchQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ProductionBatchQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ProductionBatchQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ProductionBatchQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ProductionBatchQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProductionBatchQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ProductionBatchQuery) Clone() *ProductionBatchQuery {
	if _q == nil {
		return nil
	}
	return &ProductionBatchQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]productionbatch.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.ProductionBatch{}, _q.predicates...),
		withRawMaterials:  _q.withRawMaterials.Clone(),
		withQualityChecks: _q.withQualityChecks.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithRawMaterials tells the query-builder to eager-load the nodes that are connected to
// the "raw_materials" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ProductionBatchQuery) WithRawMaterials(opts ...func(*BatchRawMaterialQuery)) *ProductionBatchQuery {
	query := (&BatchRawMaterialClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRawMaterials = query
	return _q
}

// WithQualityChecks tells the query-builder to eager-load the nodes that are connected to
// the "quality_checks" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ProductionBatchQuery) WithQualityChecks(opts ...func(*QualityCheckQuery)) *ProductionBatchQuery {
	query := (&QualityCheckClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withQualityChecks = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProductionBatch.Query().
//		GroupBy(productionbatch.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ProductionBatchQuery) GroupBy(field string, fields ...string) *ProductionBatchGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProductionBatchGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = productionbatch.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.ProductionBatch.Query().
//		Select(productionbatch.FieldTenantID).
//		Scan(ctx, &v)
func (_q *ProductionBatchQuery) Select(fields ...string) *ProductionBatchSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ProductionBatchSelect{ProductionBatchQuery: _q}
	sbuild.label = productionbatch.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProductionBatchSelect configured with the given aggregations.
func (_q *ProductionBatchQuery) Aggregate(fns ...AggregateFunc) *ProductionBatchSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ProductionBatchQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !productionbatch.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ProductionBatchQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProductionBatch, error) {
	var (
		nodes       = []*ProductionBatch{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withRawMaterials != nil,
			_q.withQualityChecks != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProductionBatch).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProductionBatch{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withRawMaterials; query != nil {
		if err := _q.loadRawMaterials(ctx, query, nodes,
			func(n *ProductionBatch) { n.Edges.RawMaterials = []*BatchRawMaterial{} },
			func(n *ProductionBatch, e *BatchRawMaterial) { n.Edges.RawMaterials = append(n.Edges.RawMaterials, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withQualityChecks; query != nil {
		if err := _q.loadQualityChecks(ctx, query, nodes,
			func(n *ProductionBatch) { n.Edges.QualityChecks = []*QualityCheck{} },
			func(n *ProductionBatch, e *QualityCheck) { n.Edges.QualityChecks = append(n.Edges.QualityChecks, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ProductionBatchQuery) loadRawMaterials(ctx context.Context, query *BatchRawMaterialQuery, nodes []*ProductionBatch, init func(*ProductionBatch), assign func(*ProductionBatch, *BatchRawMaterial)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ProductionBatch)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(batchrawmaterial.FieldProductionBatchID)
	}
	query.Where(predicate.BatchRawMaterial(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(productionbatch.RawMaterialsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ProductionBatchID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "production_batch_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ProductionBatchQuery) loadQualityChecks(ctx context.Context, query *QualityCheckQuery, nodes []*ProductionBatch, init func(*ProductionBatch), assign func(*ProductionBatch, *QualityCheck)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ProductionBatch)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(qualitycheck.FieldProductionBatchID)
	}
	query.Where(predicate.QualityCheck(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(productionbatch.QualityChecksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ProductionBatchID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "production_batch_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ProductionBatchQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ProductionBatchQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(productionbatch.Table, productionbatch.Columns, sqlgraph.NewFieldSpec(productionbatch.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, productionbatch.FieldID)
		for i := range fields {
			if fields[i] != productionbatch.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ProductionBatchQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(productionbatch.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = productionbatch.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProductionBatchGroupBy is the group-by builder for ProductionBatch entities.
type ProductionBatchGroupBy struct {
	selector
	build *ProductionBatchQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ProductionBatchGroupBy) Aggregate(fns ...AggregateFunc) *ProductionBatchGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ProductionBatchGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductionBatchQuery, *ProductionBatchGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ProductionBatchGroupBy) sqlScan(ctx context.Context, root *ProductionBatchQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProductionBatchSelect is the builder for selecting fields of ProductionBatch entities.
type ProductionBatchSelect struct {
	*ProductionBatchQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ProductionBatchSelect) Aggregate(fns ...AggregateFunc) *ProductionBatchSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ProductionBatchSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductionBatchQuery, *ProductionBatchSelect](ctx, _s.ProductionBatchQuery, _s, _s.inters, v)
}

func (_s *ProductionBatchSelect) sqlScan(ctx context.Context, root *ProductionBatchQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
