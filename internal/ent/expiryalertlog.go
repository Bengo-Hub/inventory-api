// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/inventory-service/internal/ent/expiryalertlog"
	"github.com/google/uuid"
)

// ExpiryAlertLog is the model entity for the ExpiryAlertLog schema.
type ExpiryAlertLog struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// LotID holds the value of the "lot_id" field.
	LotID uuid.UUID `json:"lot_id,omitempty"`
	// warning = within TenantInventoryConfig.expiry_warning_days; critical = within 7 days
	Tier expiryalertlog.Tier `json:"tier,omitempty"`
	// AlertedAt holds the value of the "alerted_at" field.
	AlertedAt    time.Time `json:"alerted_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ExpiryAlertLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case expiryalertlog.FieldTier:
			values[i] = new(sql.NullString)
		case expiryalertlog.FieldAlertedAt:
			values[i] = new(sql.NullTime)
		case expiryalertlog.FieldID, expiryalertlog.FieldTenantID, expiryalertlog.FieldLotID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ExpiryAlertLog fields.
func (_m *ExpiryAlertLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case expiryalertlog.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case expiryalertlog.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				_m.TenantID = *value
			}
		case expiryalertlog.FieldLotID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field lot_id", values[i])
			} else if value != nil {
				_m.LotID = *value
			}
		case expiryalertlog.FieldTier:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tier", values[i])
			} else if value.Valid {
				_m.Tier = expiryalertlog.Tier(value.String)
			}
		case expiryalertlog.FieldAlertedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field alerted_at", values[i])
			} else if value.Valid {
				_m.AlertedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ExpiryAlertLog.
// This includes values selected through modifiers, order, etc.
func (_m *ExpiryAlertLog) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ExpiryAlertLog.
// Note that you need to call ExpiryAlertLog.Unwrap() before calling this method if this ExpiryAlertLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ExpiryAlertLog) Update() *ExpiryAlertLogUpdateOne {
	return NewExpiryAlertLogClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ExpiryAlertLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ExpiryAlertLog) Unwrap() *ExpiryAlertLog {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ExpiryAlertLog is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ExpiryAlertLog) String() string {
	var builder strings.Builder
	builder.WriteString("ExpiryAlertLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("lot_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.LotID))
	builder.WriteString(", ")
	builder.WriteString("tier=")
	builder.WriteString(fmt.Sprintf("%v", _m.Tier))
	builder.WriteString(", ")
	builder.WriteString("alerted_at=")
	builder.WriteString(_m.AlertedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ExpiryAlertLogs is a parsable slice of ExpiryAlertLog.
type ExpiryAlertLogs []*ExpiryAlertLog
