// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/inventory-service/internal/ent/batchrawmaterial"
	"github.com/bengobox/inventory-service/internal/ent/productionbatch"
	"github.com/google/uuid"
)

// BatchRawMaterial is the model entity for the BatchRawMaterial schema.
type BatchRawMaterial struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// ProductionBatchID holds the value of the "production_batch_id" field.
	ProductionBatchID uuid.UUID `json:"production_batch_id,omitempty"`
	// FK to Item (raw material)
	ItemID uuid.UUID `json:"item_id,omitempty"`
	// UnitID holds the value of the "unit_id" field.
	UnitID *uuid.UUID `json:"unit_id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity float64 `json:"quantity,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BatchRawMaterialQuery when eager-loading is set.
	Edges        BatchRawMaterialEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BatchRawMaterialEdges holds the relations/edges for other nodes in the graph.
type BatchRawMaterialEdges struct {
	// ProductionBatch holds the value of the production_batch edge.
	ProductionBatch *ProductionBatch `json:"production_batch,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProductionBatchOrErr returns the ProductionBatch value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BatchRawMaterialEdges) ProductionBatchOrErr() (*ProductionBatch, error) {
	if e.ProductionBatch != nil {
		return e.ProductionBatch, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: productionbatch.Label}
	}
	return nil, &NotLoadedError{edge: "production_batch"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BatchRawMaterial) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case batchrawmaterial.FieldUnitID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case batchrawmaterial.FieldQuantity:
			values[i] = new(sql.NullFloat64)
		case batchrawmaterial.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case batchrawmaterial.FieldID, batchrawmaterial.FieldTenantID, batchrawmaterial.FieldProductionBatchID, batchrawmaterial.FieldItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BatchRawMaterial fields.
func (_m *BatchRawMaterial) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case batchrawmaterial.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case batchrawmaterial.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				_m.TenantID = *value
			}
		case batchrawmaterial.FieldProductionBatchID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field production_batch_id", values[i])
			} else if value != nil {
				_m.ProductionBatchID = *value
			}
		case batchrawmaterial.FieldItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field item_id", values[i])
			} else if value != nil {
				_m.ItemID = *value
			}
		case batchrawmaterial.FieldUnitID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field unit_id", values[i])
			} else if value.Valid {
				_m.UnitID = new(uuid.UUID)
				*_m.UnitID = *value.S.(*uuid.UUID)
			}
		case batchrawmaterial.FieldQuantity:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				_m.Quantity = value.Float64
			}
		case batchrawmaterial.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BatchRawMaterial.
// This includes values selected through modifiers, order, etc.
func (_m *BatchRawMaterial) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryProductionBatch queries the "production_batch" edge of the BatchRawMaterial entity.
func (_m *BatchRawMaterial) QueryProductionBatch() *ProductionBatchQuery {
	return NewBatchRawMaterialClient(_m.config).QueryProductionBatch(_m)
}

// Update returns a builder for updating this BatchRawMaterial.
// Note that you need to call BatchRawMaterial.Unwrap() before calling this method if this BatchRawMaterial
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BatchRawMaterial) Update() *BatchRawMaterialUpdateOne {
	return NewBatchRawMaterialClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BatchRawMaterial entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BatchRawMaterial) Unwrap() *BatchRawMaterial {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BatchRawMaterial is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BatchRawMaterial) String() string {
	var builder strings.Builder
	builder.WriteString("BatchRawMaterial(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("production_batch_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProductionBatchID))
	builder.WriteString(", ")
	builder.WriteString("item_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ItemID))
	builder.WriteString(", ")
	if v := _m.UnitID; v != nil {
		builder.WriteString("unit_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Quantity))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BatchRawMaterials is a parsable slice of BatchRawMaterial.
type BatchRawMaterials []*BatchRawMaterial
